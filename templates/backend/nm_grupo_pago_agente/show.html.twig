{% extends 'base.html.twig' %}

{% block title %}NmGrupoPagoAgente{% endblock %}

{% block body %}
    <h1>NmGrupoPagoAgente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nm_grupo_pago_agente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ nm_grupo_pago_agente.nombre }}</td>
            </tr>
            <tr>
                <th>Porcentaje</th>
                <td>{{ nm_grupo_pago_agente.porcentaje }}</td>
            </tr>
            <tr>
                <th>Utilidad</th>
                <td>{{ nm_grupo_pago_agente.utilidad }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ nm_grupo_pago_agente.created ? nm_grupo_pago_agente.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ nm_grupo_pago_agente.updated ? nm_grupo_pago_agente.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ nm_grupo_pago_agente.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nm_grupo_pago_agente_index') }}">back to list</a>

    <a href="{{ path('nm_grupo_pago_agente_edit', {'id': nm_grupo_pago_agente.id}) }}">edit</a>

    {{ include('nm_grupo_pago_agente/_delete_form.html.twig') }}
{% endblock %}
