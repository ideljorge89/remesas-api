{% extends 'base.html.twig' %}

{% block title %}NmMunicipio{% endblock %}

{% block body %}
    <h1>NmMunicipio</h1>

    <table class=" table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nm_municipio.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nm_municipio.name }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ nm_municipio.created ? nm_municipio.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ nm_municipio.updated ? nm_municipio.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nm_municipio_index') }}">back to list</a>

    <a href="{{ path('nm_municipio_edit', {'id': nm_municipio.id}) }}">edit</a>

    {{ include('nm_municipio/_delete_form.html.twig') }}
{% endblock %}
