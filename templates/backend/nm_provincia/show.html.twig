{% extends 'base_backend.html.twig' %}

{% block title %}Nmprovincia{% endblock %}

{% block body %}
    <h1>Nmprovincia</h1>

    <table class=" table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ nm_provincium.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ nm_provincium.name }}</td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ nm_provincium.created ? nm_provincia.created|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ nm_provincium.updated ? nm_provincia.updated|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('nm_provincia_index') }}">back to list</a>

    <a href="{{ path('nm_provincia_edit', {'id': nm_provincium.id}) }}">edit</a>

    {{ include('backend/nm_provincia/_delete_form.html.twig') }}
{% endblock %}
